# Model Registry

Manage trained model artifacts:

- Versioning -- Track every model version with metadata
- Tagging -- Organize by stage (dev, staging, production)
- Metrics -- Store evaluation metrics alongside artifacts
- Lineage -- Track which data produced each version
